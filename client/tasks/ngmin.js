module.exports = function (grunt){
	/**
     * `ng-min` annotates the sources before minifying. That is, it allows us
     * to code without the array syntax.
     */
    grunt.registerTask('ngmin', []);
}